{
  "name": "plasma-runner-js-eval",
  "version": "1.0.0",
  "description": "Allows running JavaScript in KRunner",
  "main": "lib/index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node lib/index.js"
  },
  "author": "opl",
  "license": "MIT",
  "dependencies": {
    "dbus-native": "^0.4.0"
  }
}
